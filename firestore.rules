rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any authenticated user to list contracts, and read/write individual ones.
    match /contracts/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
