
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /contracts/{document=**} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false; // Keep update and delete restricted for now
    }
  }
}
