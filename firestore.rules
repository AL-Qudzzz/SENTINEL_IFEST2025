
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write operations for any authenticated user.
    // This is for development purposes only and is not secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
