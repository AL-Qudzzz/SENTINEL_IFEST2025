rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read, create, and update contracts.
    match /contracts/{contractId} {
      allow get, update: if request.auth != null;
    }
    match /contracts {
      allow list, create: if request.auth != null;
    }
  }
}
